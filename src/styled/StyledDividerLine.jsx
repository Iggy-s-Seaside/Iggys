import styled from '@emotion/styled';

const StyledDividerLine = styled('hr')`
    border: none;
    height: 1px;
    background-color: #64dfdf;
    margin: 1rem 0;
`;

export default StyledDividerLine;
